server {
    listen 3000;

    location / {
        root /usr/share/nginx/html;
        index login.html;
        try_files $uri $uri/ /login.html;
    }

    location /static/ {
        root /usr/share/nginx/html;
    }
}
